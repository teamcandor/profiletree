import axios from "axios"

export default async (req, res) => {

  // 1: Use Axios to GET data from 'https://pipl.ir/v1/getPerson'

  // 2: Manipulate person's data to get only the data we want

  // 3: Send both person + (predefined) posts to frontend

  res.status(200).json({})
}
